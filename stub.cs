using System.Windows;

// stub out MIDI and TCPlistener
namespace blekenbleu.SimHub_Remote_menu
{
/*	class HttpServer
	{
		internal static string SliderProperty;
		internal static double SliderValue;

		internal static void SSEcell(int row, int col, string val) {}
		internal static void SSErespond(string s) {}
		internal static void SSEscroll(byte b) {}
		internal static void SSEslide(double v, string p) {}
		internal static void Start(Control v) {}
	}

	public class MidiDev			// must be public for Settings.cs
	{
		public string devName, butName;
		public int devMessage;	// {4-bit dev = 1-bit button | 3-bit lMidiIn index} | data2 | data 1 | status
	}

	class MIDI
	{
		internal static bool Start(ViewModel m, Control t) { return false; }
		internal static void Resume(ViewModel m, Control t) {}
		internal static bool Stop() { return false; }
	}

	public partial class Control
    {
		private void Slider_DragCompleted(object sender, System.Windows.Input.MouseButtonEventArgs e)
		{
			OK.FromSlider(0.5 + SL.Value);
		}

		internal void ButEvent(object sender, RoutedEventArgs e)
		{
			string name = (e.OriginalSource as FrameworkElement).Name;

			if ("bm" == name)           // [MIDI learn] button
				Model.MidiStatus = "\nMIDI learn disabled";
			else OK.ClickHandle(name);
		}
	}
 */
}
